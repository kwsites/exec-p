{
  "name": "@kwsites/exec-p",
  "version": "0.2.0",
  "description": "Execute a command and retrieve data as a promise.",
  "main": "./dist/exec",
  "types": "./dist/exec.d.ts",
  "files": [
    "dist/"
  ],
  "repository": "git://github.com/kwsites/exec-p.git",
  "scripts": {
    "clean": "rimraf ./dist",
    "preversion": "npm run clean && tsc",
    "postversion": "npm publish && git push && git push --tags && npm run clean"
  },
  "keywords": [
    "child process",
    "exec",
    "spawn"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^10.7.1",
    "rimraf": "^2.6.2",
    "typescript": "^3.0.1"
  },
  "peerDependencies": {}
}
